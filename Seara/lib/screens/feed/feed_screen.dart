import 'package:flutter/material.dart';

class FeedScreen extends StatelessWidget {
  @override
  /// Builds the widget tree for this view
  Widget build(BuildContext context) {
    return Center(child: Text('Bem-vindo ao Feed da Seara!'));
  }
}
