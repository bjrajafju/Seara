import 'package:flutter/material.dart';

class TaggedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Bem-vindo ao Tagged da Seara!'));
  }
}